import React from "react";
import { Container } from "react-bootstrap";

const Contacts = () => {
  return (
    <div>
      <Container className="py-100">Contacts</Container>
    </div>
  );
};
export default Contacts;
